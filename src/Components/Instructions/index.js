import React from "react";

export class Instructions extends React.Component {
    render() {
        return (
            <div>
                <p>These are the instructions - think we should be using state and props</p>
            </div>
        )
    }
}